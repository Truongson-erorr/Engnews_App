import 'package:flutter/material.dart';

class ArticleManagerPage extends StatelessWidget {
  const ArticleManagerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Quản lý bài viết",
        style: TextStyle(color: Colors.white, fontSize: 20),
      ),
    );
  }
}
